apiVersion: masha.io/v1
kind: Container
metadata:
  name: nginx
spec:
  name: test-containers
  image: nginx:latest
  resources:
    limits:
      memory: "128Mi"
      cpu: "500m"
    requests:
      memory: "64Mi"
      cpu: "250m"