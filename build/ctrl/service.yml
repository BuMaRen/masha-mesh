apiVersion: v1
kind: Service
metadata:
  name: mesh-ctrl
  namespace: default
  labels:
    app: mesh-ctrl
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: ctrl-grpc-port
    - port: 443
      targetPort: ctrl-webhook-port
  selector:
    app: mesh-ctrl