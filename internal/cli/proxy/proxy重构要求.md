# Proxy

目标：接收 app 的流量，根据配置要求进行转发或者透传

## 模块外部表现

提供 NewXXXX 函数创建实例，外部模块调用其 Run 方法开始执行代理

### 参数

一个参数用于指示 Proxy 的监听地址  
一个参数指定配置文件

### 要求

配置文件配置以下的内容：  
- TCP 流量直接透传，不可配置
- grpc 流量直接透传，不可配置
- http/https 流量可配置代理
  - 如果配置了代理：以 ip 访问不代理，以域名访问时根据该域所有 ip 做负载代理
  - 如果没有配置代理：直接透传
- 可以配置我已经写好的熔断器
